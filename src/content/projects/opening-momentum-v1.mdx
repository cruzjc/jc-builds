---
slug: "opening-momentum-v1"
title: "Opening-Momentum v1"
summary: "Runs a deterministic single-writer Raspberry Pi workflow with live Alpaca/IEX execution and fail-closed controls; adoption provenance is repaired while autonomous entries remain safety-paused."
agentSummary: "Claude only nominates bounded candidates; deterministic Alpaca/IEX rules own entries, exits, sizing, and reconciliation. Completed adoption provenance now survives full-history replay, the broker is flat, and reactivation remains an explicit operator decision."
status: "active"
surface: "listed"
featured: false
tags:
  - "python"
  - "trading"
  - "alpaca"
  - "iex"
  - "raspberry-pi"
  - "automation"
repoUrls: []
heroImage: "/art/project-template.svg"
lastUpdated: "2026-07-27"
order: 1001
labStage: "testing"
---

## Objective

Opening-Momentum v1 turns early-session US-equity data into bounded trade decisions. Claude can nominate a short list of symbols, but Alpaca/IEX market and account data plus fixed risk rules independently determine whether a setup qualifies, calculate size and prices, and control the order lifecycle. The Raspberry Pi deployment includes the Trade Operations dashboard on its HTTPS `/trade/` route and a single deterministic order path with independent authorization, ownership, reconciliation, and kill-switch controls.

## Strategy boundaries

- New entries are long-only and require the configured opening-range, VWAP, catalyst, spread, and liquidity conditions.
- Incomplete or missing opening-range data is non-actionable; the engine does not infer a trade plan from partial inputs.
- Failed-gap and opening-range failure logic protects only long positions owned by this strategy; it does not open short positions or sell unrelated holdings.
- Alpaca/IEX is the decision-time source of truth for bars, quotes, account state, positions, and market context.
- Claude may nominate at most five same-session `long_candidate` symbols. Its unattended runner exposes only web search and fetch tools; local shell, file, browser, persistence, skill, and configured MCP surfaces are disabled. Price, quantity, side, stop, target, order type, timing, command, and unknown fields are rejected.
- A new entry requires a fresh observed breakout, executable ask within a deterministic limit, current catalyst and liquidity checks, PDT and live-equity policy, a one-entry-per-session cap, and a maximum proposed notional of $50.
- Entries use whole-share DAY limit brackets after the breakout is observed. Protective exits cancel related strategy orders, re-read the live remaining long quantity, and use a risk-reducing DAY market sell. Explicit Alpaca `position_intent`, stable per-session client IDs, durable broker evidence, and reconciliation prevent blind retry.
- Trade Operations separates strategy-owned and external positions, recent automated-order activity, and recent manual-close activity. Its durable full-close workflow retains mutually exclusive strategy and external-position modes so an operator cannot apply both paths to the same action.

## Current state

The live Alpaca credentials were rotated and isolated to the required Pi services; the prior key was verified revoked. Key-only SSH remains available, while password and keyboard-interactive SSH are disabled. Competing legacy order writers and schedules are disabled or masked, leaving the guarded Opening-Momentum executor as the intended single writer.

The live cutover completed on July 24. The executor submitted one full-quantity `sell_to_close` for the adopted long, the broker confirmed the position flat, and no autonomous entry order was placed. A later cross-session reconciliation pass retained the exit fill after clearing the adoption provenance and incorrectly inferred a short even though the broker still showed zero positions and zero open orders.

The engine responded fail-closed: it latched its internal kill switch, blocked new entries, emitted a critical safety result, and exited nonzero without placing another order.

The repaired state model now archives a completed adoption as immutable provenance instead of deleting it. Full-history reconstruction seeds that adopted long basis before replaying strategy exits, so the July 24 CLSK close continues to net to zero across later sessions. Cross-session, migration, malformed-state, and overlap regressions were added; the deployed Pi suite passed 418 tests.

The Pi state was migrated from the checksum-pinned July 24 cutover snapshot while broker mutations were blocked. Two live read-only checks confirmed zero positions and zero open orders, the false local short cleared, and completed CLSK provenance remained intact. The strategy registry and reusable inventory checker now describe the actual live-mode auto and operator-exit services instead of the retired dry-run alert timer.

The current operating state is live-configured and deliberately safety-paused: live-entry authorization is off, the external and internal kill switches remain set, and the autonomous timer is disabled. Operator-exit supervision remains enabled. Re-enabling autonomous entries requires a separate, explicit operator decision; this repair did not reactivate trading.
