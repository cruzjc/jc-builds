---
slug: "pi-trade-operations"
title: "Pi Trade Operations"
summary: "Monitors independently gated trading strategies and scheduled Codex market research on Raspberry Pi while deterministic engines retain execution authority."
agentSummary: "Operational dashboard for strategy-specific controls, telemetry, and bounded Codex research. Research can recommend directional tilts; deterministic market-data, risk, and order gates remain authoritative."
status: "active"
surface: "listed"
featured: false
tags:
  - "python"
  - "trading"
  - "raspberry-pi"
  - "codex"
  - "operations"
repoUrls: []
heroImage: "/art/project-template.svg"
lastUpdated: "2026-08-22"
order: 1000
labStage: "testing"
---

## Objective

Pi Trade Operations provides one Raspberry Pi-hosted surface for monitoring independently gated trading strategies and scheduled market research. It keeps research, deterministic strategy decisions, and broker operations visibly separate so a healthy research workflow cannot silently become order authority.

## How it works

The `/trading/` dashboard combines scheduler health, strategy-specific controls, research artifacts, engine-applied threshold telemetry, and position or order state. Scheduled Codex research can nominate bounded equity or option candidates and recommend directional call, put, and event-risk actions. It cannot supply prices, quantities, order types, or commands.

Short-Term Options also has revision-checked strategy presets. The dashboard compares the fixed August 4 signal and session settings, the adaptive pre-change settings, and the active configuration side by side. Operators can apply either historical preset atomically or edit the supported controls individually, while the engine continues to enforce its non-configurable broker, ownership, reconciliation, affordability, and exit protections.

A three-way Directional threshold bias selector controls which side receives the easier of the profile's two entry hurdles. Prefer calls maps the lower hurdle to calls and the higher hurdle to puts, Neutral maps the stricter hurdle to both, and Prefer puts reverses the call-favoring mapping. It affects new-entry thresholds only: neither side is disabled and protective exits remain unchanged. The historical presets explicitly restore Prefer calls when selected.

For each new filled position, the engine freezes the effective entry threshold and a proportional take-profit target. Position management can also invalidate a long call or put from completed one-minute underlying bars: a confirmed adverse momentum reversal or loss of cumulative regular-session VWAP is latched before any resting take-profit cancellation. Reconciliation and a fresh position snapshot remain mandatory before the strategy submits a close, so the faster signal exit does not bypass ownership or duplicate-order protections.

Stop-loss execution has an independent, revision-checked market-close option. When enabled, a latched option stop first cancels any resting take profit or older stop, waits for broker-terminal reconciliation, re-reads the held quantity, and persists one idempotent exit intent before submitting a DAY market `sell_to_close`. Missing, malformed, or legacy controls retain the historical bid-limit behavior, and applying a historical signal preset preserves this safety setting instead of changing it.

Each deterministic engine independently reads current market and account data, validates the research schema and session window, applies configured multiplier bounds, and retains full authority over eligibility, risk, and execution. Strategies keep separate enablement, interlocks, timers, ownership state, and exit supervision; the dashboard does not collapse them into one generic live status.

## Current state

As verified on August 22, 2026, Short-Term Options is the selected live strategy with a custom August 4-derived configuration at control revision 12. It keeps the fixed signal profile, four completed five-minute bars, a 9:30 a.m. to 2:00 p.m. Eastern entry window, exact-ask entry pricing, and a 20-attempt daily buy guard. At an 80% threshold scale, Neutral applies the stricter scaled hurdle to both directions, producing a +0.24% call trigger and a -0.24% put trigger. The +80% option take-profit target for each new position remains unchanged, while the independently configurable stop-loss market close is enabled.

Technical exits are enabled with two-bar confirmation, a reversal threshold equal to 50% of the frozen entry trigger, and a five-basis-point VWAP-loss buffer. They require five fully post-fill one-minute bars and treat momentum reversal and VWAP loss as independent OR conditions. Missing or stale recent data resets the technical evidence without removing the resting take profit, stop loss, or timed flatten.

The historical signal behavior is layered onto the current safety architecture rather than restoring the older engine wholesale. The dynamic buying-power affordability ceiling, active-strategy gate, shared lock, durable order intents, idempotent recovery, account reconciliation, kill controls, strategy-owned exits, and timed flattening remain enforced. At the verification snapshot, the broker and strategy state were flat with no open or pending orders, the timer was enabled and healthy, and no test order had been sent.

Opening Momentum remains separately disabled and safety-blocked by its own interlocks; selecting Short-Term Options does not authorize every strategy. The strategy registry now agrees with the active systemd/live deployment and validates without errors, while three legacy registry rows still produce explicit unmatched-inventory warnings.
